# Experiments with the `pgmpy` package

`pgmpy` is a probabilistic graph modeling package for Python. This repo will include some notebooks where I try to figure it out and probably hurt myself in the process.
